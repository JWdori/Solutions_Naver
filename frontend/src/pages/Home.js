import React from "react";

const Home = (props) => {

    const onClick = () => {
    };

    return (
        <>
            누가 화면 UI 제발 만들어줘,,,
            난 여기까지다...

        </>
    );
};

export default Home;
